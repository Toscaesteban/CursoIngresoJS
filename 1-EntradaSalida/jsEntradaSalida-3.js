/*Debemos lograr tomar un dato por 'ID'
y luego mostrarlo por 'Alert' al presionar el botón  'mostrar'*/
function mostrar()
{		
	var nombre;
	nombre=document.getElementById('elNombre').value;// este id se usa para guarda en (nombre variable)
	alert(nombre);
}
/* el documenten sirve para guardar un dato donde lo pida 
		esto es mas que nada para los usarios
		*/

